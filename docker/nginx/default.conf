server {
    listen 80;
    server_name strudai.com dev.strudai.com;
    return 301 https://$host$request_uri;
}

server {
    listen 80;
    server_name _;
    return 444;
}

server {
    listen 443 ssl default_server;
    server_name _;

    ssl_certificate     /etc/ssl/cloudflare/origin.pem;
    ssl_certificate_key /etc/ssl/cloudflare/origin.key;

    return 444;
}

server {
    listen 443 ssl;
    server_name strudai.com;

    ssl_certificate     /etc/ssl/cloudflare/origin.pem;
    ssl_certificate_key /etc/ssl/cloudflare/origin.key;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    root /usr/share/nginx/html;
    index index.html;

    include /etc/nginx/security_headers.conf;

    # SPA routing
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache busted assets (Vite fingerprints filenames)
    # add_header is not inherited when a location sets its own headers,
    # so security headers must be re-included here.
    location ~* \.(js|css|woff2|png|svg|ico)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        include /etc/nginx/security_headers.conf;
    }

    # Don't cache index.html itself
    location = /index.html {
        add_header Cache-Control "no-cache";
        include /etc/nginx/security_headers.conf;
    }
}

server {
    listen 443 ssl;
    server_name dev.strudai.com;

    ssl_certificate     /etc/ssl/cloudflare/origin.pem;
    ssl_certificate_key /etc/ssl/cloudflare/origin.key;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    root /usr/share/nginx/dev-html;
    index index.html;

    include /etc/nginx/security_headers.conf;

    # SPA routing
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache busted assets (Vite fingerprints filenames)
    location ~* \.(js|css|woff2|png|svg|ico)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        include /etc/nginx/security_headers.conf;
    }

    # Don't cache index.html itself
    location = /index.html {
        add_header Cache-Control "no-cache";
        include /etc/nginx/security_headers.conf;
    }
}