// Stub for nextstepjs's optional Next.js peer dependency.
// nextstepjs uses the window adapter in this project so these are never called.
export const useRouter = () => ({});
export const usePathname = () => "/";
export const useSearchParams = () => new URLSearchParams();
